# Optional: enables Hugging Face structured-output resolution for ambiguous records.
HF_TOKEN=
HF_MODEL=Qwen/Qwen3-32B
HF_PROVIDER=auto
HF_TIMEOUT_SECONDS=20

# The Vite development server is allowed to call this FastAPI application.
FRONTEND_ORIGIN=http://localhost:5173
